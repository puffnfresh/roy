data Foo = Bar | Baz

let expensive n count =
  if n == 0 then
    console.log "expensive called " ++ (String count) ++ " times."
    Bar()
  else
    expensive (n - 1) (count + 1)

let foo = match (expensive 10000 0)
  case Bar = true
  case Baz = false
